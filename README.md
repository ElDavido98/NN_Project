# NN_Project

## Usage

[**Python 3.8+**](https://www.python.org/) is required.
```
pip install torch
pip install numpy 
pip install timm
pip install netCDF4
pip install sklearn
pip install matplotlib
pip install argparse
```
